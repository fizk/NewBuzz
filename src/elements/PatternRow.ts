import Node from './Node';

export default class PatternRow extends Node {
    get height() {
        return 20;
    }

    draw(x: number = 0, y: number = 0): void {

        super.draw(x, y);
    }
}
